<div class="header">

  <ul class="nav nav-pills pull-right">
  <% if current_user %>
    <li>
      <!-- Sign in button -->
      <div class="btn-group">
	<button type="button"
		class="btn btn-default dropdown-toggle"
		data-toggle="dropdown">
	  <%= current_user.firstname %> <span class="caret"></span>
	</button>
	<ul class="dropdown-menu" role="menu">
	  <li><%= link_to "Profile", "#" %></li>
	  <li class="divider"></li>
	  <li><%= link_to "Sign Out", signout_path %></li>
	</ul>
      </div>
    </li>
  <% else %>
    <li>
      <!-- Sign in button -->
      <div class="btn-group">
	<button type="button"
		class="btn btn-default dropdown-toggle"
		data-toggle="dropdown">
	  Sign in <span class="caret"></span>
	</button>
	<ul class="dropdown-menu" role="menu">
	  <li><%= link_to "Facebook", "/auth/facebook" %></li>
	</ul>
      </div>
    </li>
  <% end %>
  </ul>
  <h3 class="text-muted">Frecda cases</h3>

</div>
